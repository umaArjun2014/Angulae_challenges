import { DivideByPipePipe } from './divide-by-pipe.pipe';

describe('DivideByPipePipe', () => {
  it('create an instance', () => {
    const pipe = new DivideByPipePipe();
    expect(pipe).toBeTruthy();
  });
});
